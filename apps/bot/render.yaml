services:
  - type: worker
    name: adn-bot
    runtime: node
    rootDir: apps/bot
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: MINIAPP_URL
        sync: false
      - key: NODE_ENV
        value: production
